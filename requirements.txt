# Core
requests>=2.28
python-dotenv>=1.0

# Voice cloning (optional - for voice_gen)
# torch>=2.0
# torchaudio>=2.0
# soundfile>=0.12
# voxcpm_package  # VoxCPM2 voice model

# Transcription (optional - for transcriber)
# faster-whisper>=0.10
# faster-whisper-large-v3  # model auto-downloads

# BGM generation (optional - for bgm_generator)
# acestep_package  # ACE-Step 1.5 music model

# Rendering (optional - for video_renderer)
# npx hyperframes  # HyperFrames CLI (Node.js)
